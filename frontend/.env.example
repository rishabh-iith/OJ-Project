# === Vite / Frontend ===
# Dev: usually use Vite proxy (/api). If not using proxy, point to local API.
# VITE_API_BASE=/api
VITE_API_BASE=http://127.0.0.1:8000/api

# Prod: on Vercel set this to your backend HTTPS URL
# VITE_API_BASE=https://backend.codearena.icu/api
